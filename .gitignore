# ── Local working docs (never commit) ─────────────────────
# Personal notes, plans, and scratch files stay out of the repo.
# Scripts use tracked fixtures/smoke-corpus/ for smoke tests.
docs/
notes/
scratch/
*.plan.md
*.private.md

# ── Test corpora / local data ──────────────────────────────
data/
*.sqlite
*.sqlite-shm
*.sqlite-wal

# ── Node / Next.js ─────────────────────────────────────────
node_modules/
.next/
out/

# ── Rust / Tauri ───────────────────────────────────────────
src-tauri/target/
src-tauri/gen/
target/

# ── Local app data (SQLite, caches) ────────────────────────
.birdbrain/

# ── Environment / secrets ──────────────────────────────────
.env
.env.local
.env.*.local
secrets.json

# ── Editor / OS ────────────────────────────────────────────
.DS_Store
.cursor/
*.log

# ── TypeScript build artifacts ─────────────────────────────
*.tsbuildinfo
